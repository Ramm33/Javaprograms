package com.operators;

public class StringCompare {
	public static void main(String[] args) {
	        String s1 = "Java";
	        String s2 = "Python";

	        String result = (s1.equals(s2))
	                ? "Strings are Equal"
	                : "Strings are Not Equal";

	        System.out.println(result);
	    }
	}